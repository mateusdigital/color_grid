[DONE] - Start new game after the victory animation finishes to play.
[DONE] - Start new game after the defeat  animation finishes to play.
- Align the color selectors button on the bottom hud.
- Make the top hud shows the current and max moves on the game.
- Implement the options screen.
    - Implement the UI radio button.
    - Implement the color theme selectors.
    - Make the options apply to the GameOptions object.
    - Make the transitions of in / out of the screen.
- Optimize the Board update method to just update the blocks that are needed.
